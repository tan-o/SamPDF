[package]
name = "samreader-layout"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
jni = "0.21.1"
docling-pdf = { version = "1.15.0", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
